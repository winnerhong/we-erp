-- =============================================================
-- 강사 양방향 동기화용 — 마지막 동기화 스냅샷(3-way 병합 기준)
--   {name, phone, email} 의 마지막 동기화 시점 값을 저장해
--   ERP/원본 어느 쪽이 바뀌었는지 판별(충돌 감지).
-- =============================================================

alter table employees add column if not exists sync_snapshot jsonb;
