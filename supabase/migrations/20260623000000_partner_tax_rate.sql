-- =============================================================
-- 거래처별 기본 부가세율 — 통장에서 세금계산서 자동 발행 시 세액 분리에 사용.
--   default_tax_rate: 퍼센트(예: 10=과세 10%, 0=면세/영세). null=기본 10% 취급.
-- =============================================================

alter table partners add column if not exists default_tax_rate numeric(5, 2);
