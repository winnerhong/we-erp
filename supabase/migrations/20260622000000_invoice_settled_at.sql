-- =============================================================
-- 빠른 수기 정산 — 통장 매칭 없이도 '받음/지급'을 클릭 한 번으로 표시.
--   settled_at: 수기 정산일(null=미정산). 통장 연결(tax_invoice_id) 또는 settled_at 중
--   하나라도 있으면 '정산완료'로 본다.
-- =============================================================

alter table tax_invoices add column if not exists settled_at date;
